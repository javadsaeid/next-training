'use client';
import React from 'react'

export default function AddToCard() {
  return (
    <button className='btn btn-sm btn-primary' onClick={() => console.log('click')}>Add to Card</button>
  )
}
